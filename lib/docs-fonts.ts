import { Inter, JetBrains_Mono } from "next/font/google"

export const docsSans = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
})

export const docsMono = JetBrains_Mono({
  variable: "--font-jetbrains-mono",
  subsets: ["latin"],
})
